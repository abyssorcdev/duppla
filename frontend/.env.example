# =============================================================================
# Duppla Frontend — Variables de entorno
# =============================================================================
#
# Carga estas variables en Doppler (proyecto: duppla, config: dev_frontend)
# La autenticación ahora usa JWT via Google OAuth — no se necesitan API keys.
#
# =============================================================================

# URL base del backend
VITE_API_URL=http://localhost:8000
